name: dart_automerge_example
description: "Demonstrates how to use the dart_automerge plugin."
publish_to: 'none'
version: 1.0.0+1

resolution: workspace

environment:
  sdk: ^3.10.7

dependencies:
  dart_automerge:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  lints: ^6.0.0

flutter:
  uses-material-design: true
